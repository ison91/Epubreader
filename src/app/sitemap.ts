import { type MetadataRoute } from 'next'
 
export default function sitemap(): MetadataRoute.Sitemap {
  // This is now handled by src/app/sitemap.xml/route.ts to ensure correct XML formatting.
  return []
}
